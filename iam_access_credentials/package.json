{
  "name": "@aws-github-actions/iam-access-credentials",
  "version": "0.8.0",
  "description": "Configure AWS IAM access credentials for use with the AWS CLI and AWS SDKs",
  "license": "Apache-2.0",
  "author": "clowd.haus",
  "main": "index.ts",
  "scripts": {
    "compile": "esbuild index.ts --bundle --platform=node --target=node24 --minify --outfile=dist/index.js"
  },
  "dependencies": {
    "@aws-sdk/client-sts": "^3.1068.0",
    "uuid": "^14.0.0"
  }
}
