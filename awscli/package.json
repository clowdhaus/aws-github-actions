{
  "name": "@aws-github-actions/awscli",
  "version": "0.5.0",
  "description": "Wrapper for awscli",
  "main": "index.ts",
  "dependencies": {
    "@aws-github-actions/awscli-core": "^0.5.0",
    "string-argv": "^0.3.1"
  },
  "scripts": {
    "compile": "ncc build -m",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "clowd.haus",
  "license": "Apache-2.0"
}
