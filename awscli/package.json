{
  "name": "@aws-github-actions/awscli",
  "version": "0.5.1",
  "description": "Wrapper for awscli",
  "main": "index.ts",
  "dependencies": {
    "@aws-github-actions/awscli-core": "^0.5.1",
    "string-argv": "^0.3.1"
  },
  "scripts": {
    "compile": "ncc build -m",
    "lint": "eslint '*.ts' --ignore-path ../.gitignore --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "clowd.haus",
  "license": "Apache-2.0"
}
